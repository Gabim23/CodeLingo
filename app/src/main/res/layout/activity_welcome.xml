<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/profile"
    android:orientation="vertical"
    android:gravity="center"> <!-- Centra el contenido -->

    <!-- Botón para ver el ranking -->
    <Button
        android:id="@+id/viewRankingButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ver Ranking"
        android:layout_marginTop="16dp"
        android:layout_gravity="center_horizontal" /> <!-- Centrado horizontalmente -->

    <!-- Botón del perfil de usuario -->
    <Button
        android:id="@+id/profileButton"
        style="@android:style/Widget.Holo.Button.Small"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="20dp"
        android:text="Perfil"
        android:drawableStart="@drawable/ic_user"/>
    <!-- Contenedor para el contenido principal -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Texto de bienvenida -->
        <TextView
            android:id="@+id/welcomeText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Bienvenido"
            android:textSize="24sp"
            android:textColor="#FFFFFF" /> <!-- Color de texto agregado -->

        <!-- Botón para jugar -->
        <Button
            android:id="@+id/btnPlay"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="12dp"
            android:text="Jugar"
            android:textSize="18sp"
            android:layout_gravity="center" /> <!-- Centrado del botón -->
    </LinearLayout>
</LinearLayout>